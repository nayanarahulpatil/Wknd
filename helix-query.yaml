indices:
  mysite:
    include:
      - '/magazine/**'
    exclude:
      - ''
    target: /query-index
    properties:
      author:
        select: main > div:nth-of-type(2) p
        value: |
          match(e1, 'by (.*)')