indices:
  mysite:
    include:
      - '/magazine/**'
    exclude:
      - ''
    target: /query-index
    properties:
      author:
        selectAll: '.cards > h5'
        value: |
          textContent(el)
        details:
          selectAll: '.cards > p'
          value: |
          textContent(el)
        img:
          selectAll: '.cards > img'
          value: |
           attribute(el, "src")