indices:
  mysite:
    include:
      - '/magazine/**'
    exclude:
      - ''
    target: /query-index
    properties:
      author:
        selectFirst: h2
        value: |
          textContent(el)